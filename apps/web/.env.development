# Development defaults. No real backend exists yet, so the mock API is used.
# Point VITE_API_BASE_URL at a running API and set VITE_USE_MOCK_API=false to
# integrate against a real dev/staging server.
VITE_API_BASE_URL=
VITE_USE_MOCK_API=true
VITE_API_LOG_REQUESTS=true

# Development defaults. Point to the local Cloudflare Workers API running on :8787
# (started with `cd apps/api && pnpm dev`).
VITE_API_BASE_URL=http://localhost:8787
VITE_USE_MOCK_API=false
VITE_API_LOG_REQUESTS=true
